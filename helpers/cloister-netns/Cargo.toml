[package]
name = "cloister-netns"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", default-features = false, features = ["sched"] }

[profile.release]
strip = true
lto = true
